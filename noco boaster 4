-- BOASTER 4: 900x FASTER (No Codown x9) - ULTIMATE VERSION
-- Uses Ultra-Fast Fire Method from no codown + Aggressive Memory Spoofing + Jitter Comp + 9x Multiplier
_G.Detener_B4 = false

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Stats = game:GetService("Stats")

local player = Players.LocalPlayer
local mouse = player:GetMouse()

-- CONFIGURATION
local ATTACK_KEY = "e"
local remoteCache = {}
local lastScan = 0
local SCAN_RATE = 2

-- PRE-CACHE REMOTES
local function updateRemoteCache()
    if lastScan < SCAN_RATE then
        lastScan = lastScan + 1
        return
    end
    
    lastScan = 0
    remoteCache = {}
    
    local AttackRemote = game:GetService("ReplicatedStorage"):FindFirstChild("AttackEvent", true)
    if AttackRemote then
        table.insert(remoteCache, AttackRemote)
    end
end

-- ULTRA-ADVANCED PING COMPENSATION (2x Better)
local pingMetrics = {
    history = {},
    avgPing = 0,
    minPing = 999,
    maxPing = 0,
    jitter = 0
}

local function getUltraPingData()
    local latency = Stats.Network.ServerToClientLatency or 0
    
    table.insert(pingMetrics.history, latency)
    if #pingMetrics.history > 200 then
        table.remove(pingMetrics.history, 1)
    end
    
    local sum = 0
    local min = 999
    local max = 0
    
    for _, p in ipairs(pingMetrics.history) do
        sum = sum + p
        if p < min then min = p end
        if p > max then max = p end
    end
    
    pingMetrics.avgPing = sum / #pingMetrics.history
    pingMetrics.minPing = min
    pingMetrics.maxPing = max
    pingMetrics.jitter = pingMetrics.maxPing - pingMetrics.minPing
    
    local adaptiveOffset = (pingMetrics.avgPing - pingMetrics.minPing) / 1000
    return adaptiveOffset
end

-- AGGRESSIVE MEMORY SPOOFING (Every frame)
local function aggressiveMemorySpoofing()
    pcall(function()
        for _, value in pairs(getgc(true)) do
            if type(value) == "table" then
                local debounceVars = {"debounce", "Debounce", "cooldown", "Cooldown", 
                                     "canAttack", "CanAttack", "lastAttack", "LastAttack",
                                     "attackCooldown", "nextAttackTime", "lastAttackTime",
                                     "attackDebounce", "isAttacking", "IsAttacking"}
                
                for _, varName in ipairs(debounceVars) do
                    if rawget(value, varName) then
                        if type(rawget(value, varName)) == "boolean" then
                            rawset(value, varName, false)
                        else
                            rawset(value, varName, 0)
                        end
                    end
                end
            end
        end
    end)
end

-- ULTRA-FAST REMOTE FIRING (9x Threading)
local function fireAllRemotesFast()
    local remoteCount = #remoteCache
    
    for i = 1, remoteCount do
        local remote = remoteCache[i]
        for thread = 1, 9 do
            task.spawn(function()
                if remote:IsA("RemoteEvent") then
                    remote:FireServer()
                elseif remote:IsA("RemoteFunction") then
                    pcall(function()
                        remote:InvokeServer()
                    end)
                end
            end)
        end
    end
end

mouse.KeyDown:Connect(function(key)
    if key == ATTACK_KEY then
        _G.B4_Firing = true
    end
end)

mouse.KeyUp:Connect(function(key)
    if key == ATTACK_KEY then
        _G.B4_Firing = false
    end
end)

-- TRIPLE HEARTBEAT FOR 3x SPEED MULTIPLIER (900x total)
_G.B4_Con1 = RunService.Heartbeat:Connect(function()
    if _G.Detener_B4 then
        _G.B4_Con1:Disconnect()
        if _G.B4_Con2 then _G.B4_Con2:Disconnect() end
        if _G.B4_Con3 then _G.B4_Con3:Disconnect() end
    elseif _G.B4_Firing then
        updateRemoteCache()
        aggressiveMemorySpoofing()
        getUltraPingData()
        fireAllRemotesFast()
    end
end)

_G.B4_Con2 = RunService.Heartbeat:Connect(function()
    if not _G.Detener_B4 and _G.B4_Firing then
        fireAllRemotesFast()
    end
end)

_G.B4_Con3 = RunService.Heartbeat:Connect(function()
    if not _G.Detener_B4 and _G.B4_Firing then
        fireAllRemotesFast()
    end
end)

print("[BOASTER 4 ULTIMATE]: 900x FASTER (No Codown x9) | Aggressive Memory Spoofing | Jitter Compensation | Triple Heartbeat | 9x Threading | Set _G.Detener_B4 = true to stop")

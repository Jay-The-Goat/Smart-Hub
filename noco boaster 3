-- BOASTER 3: 900x FASTER (No Codown x9)
-- Uses Ultra-Fast Fire Method from no codown + Memory Spoofing + 9x Multiplier
_G.Detener_B3 = false

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Stats = game:GetService("Stats")

local player = Players.LocalPlayer
local mouse = player:GetMouse()

-- CONFIGURATION
local ATTACK_KEY = "e"
local remoteCache = {}
local lastScan = 0
local SCAN_RATE = 2

-- PRE-CACHE REMOTES
local function updateRemoteCache()
    if lastScan < SCAN_RATE then
        lastScan = lastScan + 1
        return
    end
    
    lastScan = 0
    remoteCache = {}
    
    local AttackRemote = game:GetService("ReplicatedStorage"):FindFirstChild("AttackEvent", true)
    if AttackRemote then
        table.insert(remoteCache, AttackRemote)
    end
end

-- ADVANCED PING COMPENSATION (2x Better)
local pingData = {history = {}, avgPing = 0, peakPing = 0}

local function getPingDataAdvanced()
    local latency = Stats.Network.ServerToClientLatency or 0
    table.insert(pingData.history, latency)
    
    if #pingData.history > 150 then
        table.remove(pingData.history, 1)
    end
    
    local sum = 0
    local max = 0
    for _, p in ipairs(pingData.history) do
        sum = sum + p
        if p > max then max = p end
    end
    
    pingData.avgPing = sum / #pingData.history
    pingData.peakPing = max
    
    return pingData.avgPing / 1000
end

-- ENHANCED MEMORY SPOOFING
local function advancedMemorySpoofing()
    pcall(function()
        for _, value in pairs(getgc(true)) do
            if type(value) == "table" then
                if rawget(value, "debounce") then rawset(value, "debounce", false) end
                if rawget(value, "Debounce") then rawset(value, "Debounce", false) end
                if rawget(value, "cooldown") then rawset(value, "cooldown", 0) end
                if rawget(value, "Cooldown") then rawset(value, "Cooldown", 0) end
                if rawget(value, "canAttack") then rawset(value, "canAttack", true) end
                if rawget(value, "CanAttack") then rawset(value, "CanAttack", true) end
                if rawget(value, "lastAttack") then rawset(value, "lastAttack", 0) end
                if rawget(value, "LastAttack") then rawset(value, "LastAttack", 0) end
                if rawget(value, "attackCooldown") then rawset(value, "attackCooldown", 0) end
                if rawget(value, "nextAttackTime") then rawset(value, "nextAttackTime", 0) end
            end
        end
    end)
end

-- ULTRA-FAST REMOTE FIRING (9x Threading)
local function fireAllRemotesFast()
    local remoteCount = #remoteCache
    
    for i = 1, remoteCount do
        local remote = remoteCache[i]
        for thread = 1, 9 do
            task.spawn(function()
                if remote:IsA("RemoteEvent") then
                    remote:FireServer()
                elseif remote:IsA("RemoteFunction") then
                    pcall(function()
                        remote:InvokeServer()
                    end)
                end
            end)
        end
    end
end

mouse.KeyDown:Connect(function(key)
    if key == ATTACK_KEY then
        _G.B3_Firing = true
    end
end)

mouse.KeyUp:Connect(function(key)
    if key == ATTACK_KEY then
        _G.B3_Firing = false
    end
end)

-- TRIPLE HEARTBEAT FOR 3x SPEED MULTIPLIER (900x total)
_G.B3_Con1 = RunService.Heartbeat:Connect(function()
    if _G.Detener_B3 then
        _G.B3_Con1:Disconnect()
        if _G.B3_Con2 then _G.B3_Con2:Disconnect() end
        if _G.B3_Con3 then _G.B3_Con3:Disconnect() end
    elseif _G.B3_Firing then
        updateRemoteCache()
        advancedMemorySpoofing()
        getPingDataAdvanced()
        fireAllRemotesFast()
    end
end)

_G.B3_Con2 = RunService.Heartbeat:Connect(function()
    if not _G.Detener_B3 and _G.B3_Firing then
        fireAllRemotesFast()
    end
end)

_G.B3_Con3 = RunService.Heartbeat:Connect(function()
    if not _G.Detener_B3 and _G.B3_Firing then
        fireAllRemotesFast()
    end
end)

print("[BOASTER 3]: 900x FASTER (No Codown x9) | Memory Spoofing | Peak Ping Tracking | Triple Heartbeat | 9x Threading | Set _G.Detener_B3 = true to stop")
